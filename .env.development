# Origins allowed to embed the tool at /api/. Comma-separated, no trailing slash.
# Empty => embed mode is off and standalone is unaffected.
# Production origins go in the Pages workflow, not here.
VITE_ALLOWED_PARENT_ORIGINS=http://localhost:5173
